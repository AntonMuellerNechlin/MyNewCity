package com.example.mynewcity.model

import org.osmdroid.util.GeoPoint

object GridConfig {

    const val CELL_SIZE_METERS = 10.0

    // fester Referenzpunkt (WICHTIG!)
    val ORIGIN = GeoPoint(52.5200, 13.4050)
}